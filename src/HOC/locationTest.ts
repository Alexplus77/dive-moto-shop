import React, { useEffect } from "react";
import { useLocation, useParams } from "react-router-dom";

interface Props {
  children: JSX.Element;
}

export const LocationTest = ({ children }: Props) => {
  return children;
};
